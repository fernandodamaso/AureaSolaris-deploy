# AureaSolaris deployment mirror bootstrap

This repository is the Vercel deployment mirror for `vivicabsb-eng/AureaSolaris`.

The `mirror-control` branch owns synchronization automation. The `main` branch is replaced by the exact upstream `main` Git ref and must not be edited directly.
