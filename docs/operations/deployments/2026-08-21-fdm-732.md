# FDM-732 hosted preview verification

Date: 2026-08-21

## Historical/superseded exact-SHA deployments

This first block records the earlier `d9244118679c7cc1d22420f10149f78815c03ca4` acceptance only as audit history. It is superseded and is not the current candidate or deployment pair. The current final candidate and deployments are recorded below under `Final Sol review fixes`.

- Commit: `d9244118679c7cc1d22420f10149f78815c03ca4`
- Web project: `aurea-solaris`
  - Deployment: `dpl_AH5MR4TZrmo89ka1NYnhikMdDxk9`
  - URL: `https://aurea-solaris-6atoo9hok-fernando-damasos-projects.vercel.app`
  - Branch alias: `https://aurea-solaris-git-preview-fernando-damasos-projects.vercel.app`
- API project: `aurea-solaris-api`
  - Deployment: `dpl_A6PDzs922VZeY5JU8VSQ4w74zc67`
  - URL: `https://aurea-solaris-otx4oq09n-fernando-damasos-projects.vercel.app`
  - Branch alias: `https://aurea-solaris-api-git-preview-fernando-damasos-projects.vercel.app`

## Provider boundary

- Preview Supabase ref: `rosklqnnbmhowohoyboj`
- Production Supabase ref: `tgpcpxqqusehssaihvcp`
- Committed migration SHA256: `42d3b1f57a52ae3fff45a0086075518a18d8924f6deb5cf7d5b1143aef46dcb2`
- Preview and production public sign-up: disabled.
- Credentials were supplied through process-only secure variables. No credential, token, payload, or protected deployment secret is stored in this record.

## Verification

The following checks passed against the matching preview aliases:

- API health: HTTP 200.
- API readiness: HTTP 503 with the expected fail-closed `service_not_ready` response.
- Unauthenticated `/v1/me`: HTTP 401.
- Authenticated `/v1/me`: HTTP 404 with the expected missing-profile response for the disposable E2E identity.
- Swiss Ephemeris natal calculation: HTTP 200 with non-empty engine and ephemeris metadata.
- Preview web origin CORS preflight: HTTP 200 with the exact preview `Access-Control-Allow-Origin`.
- Public Supabase sign-up: HTTP 422 with `signup_disabled`.
- Authenticated natal receipt creation: HTTP 200.
- Authenticated transit receipt creation: HTTP 200.
- Receipt access without a token: HTTP 401.
- Receipt access with a different authenticated user: HTTP 404 with `receipt_not_found`.
- Browser flow: login, onboarding, natal/transit receipt flow, dashboard reload persistence, and logout.
- Browser request policy: no HTTP, localhost, loopback, production API, or production Supabase requests observed.
- Browser console/page errors: none observed by the ownership test.

The repeatable command is `scripts/verify_preview.sh`; it prints status lines only and invokes the ownership test with tracing, screenshots, and video disabled.

## Final Sol review fixes

- Final application candidate: `34fa93893f50a0dd4fbe162ddc9159faa422b722`.
- Upstream `codex/fdm-732-remediation` and mirror `preview` are equal at that SHA.
- Final READY web deployment: `dpl_e8E8yrnTR5i5KBwTCQM4w952QQ3r`.
- Final READY API deployment: `dpl_JB4mDAJqFnrazh4En8DzMs3J1WvH`.
- Both deployments report candidate SHA `34fa93893f50a0dd4fbe162ddc9159faa422b722` and preview ref metadata.
- The exact-SHA API verifier accepted the unique READY preview deployment and emitted only its HTTPS URL. The direct protected API smoke was not repeated because this candidate changes no hosted runtime path; prior credentialed runtime evidence is carried forward for that bounded reason.
- The candidate fixes exact `SUPABASE_PREVIEW_URL` validation with one trailing-slash normalization, makes the real curl regression portable, and catalogs `protection-boundary` under the checked-in E2E schema. Missing and fake origins stop before curl or browser launch; exact and normalized values pass.
- Full Git Bash quality gate passed: web 44 files/213 tests; API 134 passed and 5 skipped; Supabase 68 checks; remaining suites 5 and 12 passed. Focused preview Python tests passed 8; controlled protection Playwright passed 1; shell syntax and classification audit passed.
- Prior accepted runtime evidence remains bound to the unchanged application path: secure wrapper PASS, separately rendered natal and transit provenance, reload persistence, unauthenticated `401`, cross-owner `404`, and public sign-up disabled. Preview ref `rosklqnnbmhowohoyboj` and production ref `tgpcpxqqusehssaihvcp` remained `ACTIVE_HEALTHY`; migration SHA256 is `42d3b1f57a52ae3fff45a0086075518a18d8924f6deb5cf7d5b1143aef46dcb2`; RLS and owner policies passed; final Auth counts were preview `39`, production `1`, with orphan aggregate `0`.
- Production and both `main` refs remain unchanged at `6ddda7627e9634e91fa303e296dec79fd93b9340`.
- The previous docs-only evidence child `af2b2f31458080c6371c74463d0851be84323a1f` remains preserved locally and is not part of this application candidate. This section is the sole new docs-only child for the final review fix.
- The app repository's `origin/preview` was an unintended mirror branch created during the prior round and remains at the candidate SHA; it was not rewound. The actual deployment mirror `fernandodamaso/AureaSolaris-deploy:preview` was fast-forwarded from `e7a86a9` to the candidate in this round. This is recorded as a sanitized workflow concern.
